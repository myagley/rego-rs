use crate::Location;
use crate::lexer::Error;
use crate::token::Token;

grammar<'input>(input: &'input str);

pub Expr: () = {
    "null"
};

extern {
    type Location = Location;
    type Error = Error;

    enum Token<'input> {
        "null" => Token::Null,
    }
}
